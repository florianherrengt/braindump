import React from 'react';
import { CircularProgress } from '.';
import '../../styles/index.scss';

export default {
    component: CircularProgress,
    title: 'CircularProgress',
};

export const Default = () => <CircularProgress />;
